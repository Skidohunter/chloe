<form method="post" action="{{ path('app_locations_delete', {'id': loc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loc.id) }}">
    <button class="btn">Delete</button>
</form>
