{% extends 'base.html.twig' %}

{% block title %}Hello LocationsController!{% endblock %}

{% block body %}
<h1>Réalisations</h1>
{% if is_granted('ROLE_ADMIN') %}
    <section class="sectionForm">
        <div class='form'>
            {{form(form)}}
        </div>
    </section>
{% endif %}
<section id="indexPresta">
    {% for real in reals %}
        <article class="indexArticle">
            <h3>{{real.name}}</h3>
            <p>{{real.description}}</p>
            <p>{{real.id}}</p>
        {% if is_granted('ROLE_USER') %}
            <a href="{{path('app_commentaire')}}">Ajout d'un Commentaire</a>
        {% endif %}
        </article>      
    {% endfor %}
</section>

{% endblock %}
